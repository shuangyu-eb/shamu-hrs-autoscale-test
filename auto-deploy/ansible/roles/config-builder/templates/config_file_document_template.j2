server:
  port: 9001

spring:
  thymeleaf:
    cache: false

cloud:
  aws:
    region:
      auto: true

aws:
  bucketName: {{stack_prefix}}-{{env}}-document-test
  folder: media

auth0:
  customNamespace: {{auth0_namespace}}

helloSign:
  testMode: true
